
  <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <h4>Welcome <span class="member-name"></span></h4>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/members">Home <span class="sr-only">(current)</span></a>
        </li>
      </ul>
      <a class="nav-link logout-link" href="/logout"><span class="navbar-text"><i class="fas fa-sign-out-alt"></i> Logout</a>
    </div>
  </nav>
<div class="container page-container">
    
    <!-- front window container -->
    <div class="container front-window">
      <h1>Choose the best that describes your feeling right now:</h1>
     <div class="emoBtn">
      
      <div class="row">
        <div class="col-lg-4 col-sm-12">
          <!-- button for happy -->
            <button type="button" class="btn btn-block btn-lg btn-outline-primary" id="happy-btn">Happy</button>
        </div>
        <div class="col-lg-4 col-sm-12">
          <!-- button for neutral -->
          <button type="button" class="btn btn-block btn-lg btn-outline-primary" id="neutral-btn">Neutral</button>
        </div>
        <div class="col-lg-4 col-sm-12">
          <!-- button for sad -->
          <button type="button" class="btn btn-block btn-lg btn-outline-primary" id="sad-btn">Sad</button>
        </div>
      </div> <!-- /row -->
      
      
      
      
      
      
      
      
      
      
    </div>
    </div>

     

    <!-- emojis container -->
    <div class="containerE overflow-auto" id="happy-emoji-container">
      <button class="x-button"><i class="fas fa-times"></i></button><br>
        {{#each positivePolarityEmojis}}
            <span class="emoji-info" data-id="{{id}}" data-name={{name}}>{{emoji}}</span>
        {{/each}}
    </div>


    <div class="containerE overflow-auto" id="neutral-emoji-container">
      <button class="x-button"><i class="fas fa-times"></i></button><br>
        {{#each neutralPolarityEmojis}}
            <span class="emoji-info" data-id="{{id}}" data-name={{name}}>{{emoji}}</span>
        {{/each}}
    </div>

  
  <div class="containerE overflow-auto" id="sad-emoji-container">
    <button class="x-button"><i class="fas fa-times"></i></button><br>
      {{#each negativePolarityEmojis}}
          <span class="emoji-info" data-id="{{id}}" data-name={{name}}>{{emoji}}</span>
      {{/each}}
  </div>

</div>


<!-- The Modal -->
<div class="modal" tabindex="1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title"><i class="fas fa-exclamation-triangle"></i> Are you sure with this emoji?</h5>
      </div>
      <div class="modal-body">
        <p id="emoji-icon"></p>
      </div>
      <div class="modal-footer">
        <!-- close button -->
        <button type="button" id="close-btn" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
        <!-- function for this button is still in need -->
        <button type="button" id="confirm-btn" class="btn btn-primary"></button>
      </div>
    </div>
  </div>
</div>
